import { JoinAida } from '@/components/club/JoinAida';

export const metadata = {
  title: 'Join AIDA — AI & Data Science Club',
  description: 'How to join the AI & Data Science Club at Ajman University through ORS.',
};

export default function ClubPage() {
  return <JoinAida />;
}
